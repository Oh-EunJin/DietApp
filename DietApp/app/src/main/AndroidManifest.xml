<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pop_exercise"
            android:theme="@style/AppTheme.AlertDialog"></activity>
        <activity android:name=".ExChoActivity" />
        <activity
            android:name=".pop_food"
            android:theme="@style/AppTheme.AlertDialog" />
        <activity android:name=".FoChoActivity" />
        <activity android:name=".foodMainActivity" />
        <activity
            android:name=".login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".join" />
        <activity android:name=".food" />
        <activity android:name=".exercise" />
        <activity
            android:name=".MainActivity"
            android:label="마이페이지"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>